
/**
 * _islower - checks for lowercase character
 * c - input that is to be checked
 *
 * Returns: void
 */

int _islower(int c)
{
	if (c >= 'a' && c <= 'z')
	{
		putchar(1);
	}
	else
	{
		putchar(0);
	}
}
